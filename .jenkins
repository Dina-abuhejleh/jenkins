pipeline {
    agent any
    stages {
        stage('Build') {
            steps { 
                 sh 'hostname'
                 sh 'free'
                 echo "Running ${env.BUILD_ID}"
            }
        }     
    }
}
