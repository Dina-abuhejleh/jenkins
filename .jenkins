pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                 sh "chmod +x -R ${/root/script/hello.sh}"
                 sh "/root/script/hello.sh"
            }
        }     
    }
}
