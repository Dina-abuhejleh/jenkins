pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                 sh "hostname"
                 sh 'echo $PATH'
                 sh '/usr/bin/free'
            }
        }     
    }
}
