pipeline {
    agent any
    stages {
        stage('Build') {
            steps { 
                 sh 'hostname'
                 echo "Running ${env.BUILD_ID}"
            }
        }     
    }
}
