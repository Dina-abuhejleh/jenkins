pipeline {
    agent any
    stages {
           stage("set variables"){
               steps{
                   script{
                       env.city="dee"
                   }
               }
           }
        stage('Build') {
            steps {
                 sh 'hostname'
                 sh 'free -m'
                 echo "Running ${env.BUILD_ID}"
                 sh 'w'
            }
        }     
    }
}
