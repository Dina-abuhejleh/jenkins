pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                 sh "hostname"
                 sh "cd / "
                 sh 'echo $PATH'
                 free
            }
        }     
    }
}
