pipeline {
    agent any
    stages {
        stage('Build') {
            steps { 
                 sh 'hostname'
                 sh 'free -m'
                 echo "Running ${env.BUILD_ID}"
                 sh 'who'
            }
        }     
    }
}
